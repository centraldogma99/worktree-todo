# Electron-builder requires a flat node_modules layout so that asar can
# resolve every transitive runtime dependency (e.g. electron-store's `conf`).
# pnpm's default symlinked layout hides hoisted deps from the packaged app.
node-linker=hoisted
